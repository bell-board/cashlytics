{
  "name": "cashlytics",
  "version": "1.0.0",
  "type": "module",
  "description": "Frontend-only expense tracker",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.1.0",
    "@vitest/coverage-v8": "^1.6.0",
    "fast-check": "^3.19.0"
  }
}
